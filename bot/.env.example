# Only scripts/deploy_commands.py reads these. The Lambda functions get what
# they need from Terraform and authenticate to AWS through their execution role.
DISCORD_APPLICATION_ID=
DISCORD_BOT_TOKEN=
