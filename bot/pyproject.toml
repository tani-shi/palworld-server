[project]
name = "palworld-bot"
version = "0.1.0"
description = "Discord bot that controls the Palworld dedicated server EC2 instance"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34",
    "discord.py>=2.3",
    "python-dotenv>=1.0",
]
